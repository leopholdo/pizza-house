import * as categories from './categoriesService';
import * as products from './productsService';
import * as additionalItems from './additionalItemsService'

export const services = {
  categories,
  products,
  additionalItems
}